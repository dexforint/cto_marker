# Архитектура Marker

## Обзор

Marker - это мощный движок для конвертации документов, написанный на Python 3.10+. Система преобразует PDF, изображения, DOCX, PPTX, XLSX, HTML и EPUB файлы в форматы Markdown, JSON, HTML и структурированные блоки данных с высокой точностью.

## Основная философия

Marker построен на модульной архитектуре, где каждый компонент отвечает за конкретную задачу в pipeline обработки документов. Система использует следующий поток данных:

```
Исходный документ → Provider → Builder → Processor → Renderer → Результат
```

## Ключевые компоненты

### 1. **Providers (Поставщики данных)** 
📁 `marker/providers/`

**Назначение:** Извлекают низкоуровневую информацию из исходных файлов.

**Основные классы:**
- `PdfProvider` - работа с PDF файлами
- `ImageProvider` - работа с изображениями
- `DocxProvider` - работа с документами Word
- `PptxProvider` - работа с презентациями PowerPoint
- `EpubProvider` - работа с электронными книгами
- `HtmlProvider` - работа с HTML файлами

**Что делают:**
- Открывают и читают файлы
- Извлекают страницы, изображения, метаданные
- Предоставляют унифицированный интерфейс для разных форматов
- Определяют характеристики документа (размеры страниц, количество страниц и т.д.)

**Реестр:** `ProviderRegistry` в `marker/providers/registry.py` автоматически выбирает подходящий Provider на основе расширения файла.

---

### 2. **Builders (Строители)** 
📁 `marker/builders/`

**Назначение:** Создают начальную структуру документа из данных Provider.

**Основные классы:**

#### `DocumentBuilder`
- Создает объект `Document` - корневой контейнер
- Инициализирует структуру страниц
- Управляет метаданными документа

#### `LayoutBuilder`
- Определяет макет страниц (layout detection)
- Использует модель Surya для распознавания структуры
- Идентифицирует блоки: текст, таблицы, рисунки, формулы и т.д.
- Создает базовые Block объекты с bounding box координатами

#### `LineBuilder`
- Определяет строки текста внутри блоков
- Использует модель Surya для детекции строк
- Группирует текст в логические строки

#### `OcrBuilder`
- Выполняет оптическое распознавание символов (OCR)
- Использует модель Surya OCR для извлечения текста
- Поддерживает множество языков
- Определяет, когда нужен OCR (отсканированные документы, плохой текст)

#### `StructureBuilder`
- Создает иерархическую структуру документа
- Связывает блоки в родительско-дочерние отношения
- Формирует дерево блоков

**Поток работы Builders:**
```
DocumentBuilder → LayoutBuilder → LineBuilder → OcrBuilder → StructureBuilder
```

---

### 3. **Processors (Процессоры)** 
📁 `marker/processors/`

**Назначение:** Обрабатывают и улучшают блоки документа, добавляя семантическую информацию.

Processors - это сердце системы обработки. Каждый processor специализируется на конкретном типе контента.

#### **Базовые процессоры:**

**`OrderProcessor`**
- Определяет порядок чтения блоков (reading order)
- Важен для многоколоночных документов
- Сортирует блоки в правильной последовательности

**`LineMergeProcessor`**
- Объединяет строки в абзацы
- Определяет границы параграфов

**`BlockRelabelProcessor`**
- Переклассифицирует блоки для более точного определения типа
- Исправляет ошибки layout detection

**`TextProcessor`**
- Обрабатывает обычные текстовые блоки
- Форматирует параграфы

**`SectionHeaderProcessor`**
- Идентифицирует заголовки разделов
- Определяет уровни заголовков (H1, H2, H3...)

**`ListProcessor`**
- Распознает списки (нумерованные и маркированные)
- Создает иерархическую структуру списков

**`TableProcessor`**
- Обрабатывает таблицы
- Извлекает структуру ячеек

**`EquationProcessor`**
- Обрабатывает математические формулы
- Конвертирует в LaTeX

**`CodeProcessor`**
- Распознает блоки кода
- Сохраняет форматирование

**`FootnoteProcessor`**
- Обрабатывает сноски

**`ReferenceProcessor`**
- Обрабатывает библиографические ссылки

**`PageHeaderProcessor`**
- Удаляет повторяющиеся колонтитулы

**`DocumentTOCProcessor`**
- Создает оглавление документа

**`DebugProcessor`**
- Сохраняет отладочную информацию (при включенном режиме debug)

#### **LLM-процессоры (опциональные, с флагом --use_llm):**
📁 `marker/processors/llm/`

Используют большие языковые модели для повышения точности:

**`LLMTableProcessor`**
- Улучшает форматирование таблиц с помощью LLM
- Исправляет ошибки структуры

**`LLMTableMergeProcessor`**
- Объединяет таблицы, разбитые на несколько страниц

**`LLMFormProcessor`**
- Извлекает данные из форм

**`LLMImageDescriptionProcessor`**
- Генерирует описания изображений

**`LLMEquationProcessor`**
- Улучшает распознавание формул

**`LLMHandwritingProcessor`**
- Распознает рукописный текст

**`LLMMathBlockProcessor`**
- Обрабатывает блоки с математикой

**`LLMSectionHeaderProcessor`**
- Улучшает распознавание заголовков

**`LLMPageCorrectionProcessor`**
- Исправляет общие ошибки на странице с помощью custom prompt

**`LLMComplexRegionProcessor`**
- Обрабатывает сложные регионы с mixed content

**Архитектура LLM процессоров:**
- Базовый класс: `LLMSimpleBlockMetaProcessor`
- Отправляет изображения/текст в LLM API
- Парсит ответы
- Обновляет метаданные блоков

---

### 4. **Schema (Схемы данных)** 
📁 `marker/schema/`

**Назначение:** Определяют структуры данных для представления документов.

#### **BlockTypes (Типы блоков)**
`marker/schema/__init__.py`

Перечисление всех возможных типов блоков:
- `Document` - весь документ
- `Page` - страница
- `Text` - обычный текст
- `SectionHeader` - заголовок раздела
- `Table` - таблица
- `TableCell` - ячейка таблицы
- `Figure` - рисунок/диаграмма
- `Picture` - изображение
- `Caption` - подпись
- `Equation` - формула
- `Code` - код
- `ListItem` - элемент списка
- `ListGroup` - группа списка
- `Form` - форма
- `Footnote` - сноска
- `Reference` - ссылка
- `Line` - строка текста
- `Span` - фрагмент текста
- `Char` - символ
- `PageHeader` - верхний колонтитул
- `PageFooter` - нижний колонтитул
- `TableOfContents` - оглавление
- `Handwriting` - рукописный текст
- `TextInlineMath` - текст с inline формулами
- `ComplexRegion` - сложный регион
- И другие...

#### **Block (Базовый класс блока)**
`marker/schema/blocks/base.py`

Все блоки наследуются от `Block`:
- `id` - уникальный идентификатор
- `block_type` - тип блока (из BlockTypes)
- `polygon` - координаты (4 угла)
- `structure` - структурные метаданные
- `children` - дочерние блоки (иерархия)
- `page_id` - ID страницы

Методы:
- `render()` - рендеринг в HTML/Markdown
- `contained_blocks()` - поиск вложенных блоков по типу
- `assemble_html()` - сборка HTML из дерева блоков

#### **Document (Класс документа)**
`marker/schema/document.py`

Корневой контейнер:
- Содержит все страницы
- Хранит метаданные
- Управляет иерархией блоков

#### **Page (Класс страницы)**
`marker/schema/blocks/page.py`

Представляет одну страницу:
- Размеры страницы
- Дочерние блоки
- Метаданные извлечения (method: pdftext/ocr/heuristic)

---

### 5. **Renderers (Рендереры)** 
📁 `marker/renderers/`

**Назначение:** Преобразуют структуру блоков в выходные форматы.

#### **MarkdownRenderer**
- Конвертирует в Markdown
- Форматирует таблицы
- Вставляет LaTeX формулы
- Создает ссылки на изображения
- По умолчанию используется этот renderer

#### **JSONRenderer**
- Конвертирует в древовидный JSON
- Сохраняет полную структуру блоков
- Включает bounding boxes
- Содержит метаданные

#### **HTMLRenderer**
- Генерирует HTML
- Использует семантические теги
- Встраивает стили

#### **ChunkedRenderer**
- Создает плоский список блоков (chunks)
- Для использования в RAG системах
- Каждый chunk - самодостаточный фрагмент с полным HTML

---

### 6. **Converters (Конвертеры)** 
📁 `marker/converters/`

**Назначение:** Управляют полным pipeline конвертации.

#### **BaseConverter**
Базовый класс для всех конвертеров:
- Координирует работу всех компонентов
- Управляет конфигурацией
- Загружает модели
- Запускает builders → processors → renderer

#### **PdfConverter** (основной)
Полный pipeline для PDF:
1. Инициализация Provider
2. Запуск Builders (layout, line, OCR, structure)
3. Выполнение Processors в заданном порядке
4. Рендеринг результата
5. Сохранение изображений

Поддерживает все форматы вывода (markdown, json, html, chunks).

#### **TableConverter**
Специализированный конвертер для таблиц:
- Извлекает только таблицы
- Оптимизирован для точности таблиц
- Может использовать LLM для улучшения

#### **OCRConverter**
Только OCR, без layout detection:
- Простой OCR текста
- Без структурирования
- Быстрый режим

#### **ExtractionConverter** (beta)
Структурированное извлечение данных:
- Использует LLM для извлечения
- Принимает JSON schema (Pydantic модель)
- Возвращает структурированные данные по schema

---

### 7. **Services (Сервисы LLM)** 
📁 `marker/services/`

**Назначение:** Интеграция с LLM API для улучшения качества.

#### **BaseService**
Базовый интерфейс для всех LLM сервисов.

#### **Реализации:**

**`GoogleGeminiService`**
- Использует Gemini API
- По умолчанию: `gemini-2.0-flash`
- Требует: `--gemini_api_key`

**`GoogleVertexService`**
- Использует Google Vertex AI
- Более надежный для production
- Требует: `--vertex_project_id`

**`OllamaService`**
- Локальные модели через Ollama
- Настройки: `--ollama_base_url`, `--ollama_model`

**`ClaudeService`**
- Anthropic Claude API
- Настройки: `--claude_api_key`, `--claude_model_name`

**`OpenAIService`**
- OpenAI API или совместимые endpoints
- Настройки: `--openai_api_key`, `--openai_model`, `--openai_base_url`

**`AzureOpenAIService`**
- Azure OpenAI Service
- Настройки: `--azure_endpoint`, `--azure_api_key`, `--deployment_name`

**Функциональность:**
- Отправка изображений с промптами
- Парсинг структурированных ответов
- Обработка ошибок и retry logic

---

### 8. **Configuration (Конфигурация)** 
📁 `marker/config/`

#### **ConfigParser**
`marker/config/parser.py`

Центральная система конфигурации:
- Парсит CLI аргументы
- Читает JSON конфигурацию
- Создает словарь настроек
- Инстанцирует компоненты (processors, renderer, llm_service)

#### **Settings**
`marker/settings.py`

Глобальные настройки на основе Pydantic:
- Пути к моделям
- Настройки GPU/CPU
- Параметры по умолчанию
- Environment variables

---

### 9. **Models (Модели)** 
📁 `marker/models.py`

**`create_model_dict()`**
Создает словарь с загруженными моделями Surya:
- `layout` - модель распознавания макета
- `order` - модель определения порядка чтения
- `recognition` - модель OCR
- `detection` - модель детекции текста
- `table_rec` - модель распознавания таблиц

Модели загружаются один раз и переиспользуются для всех документов.

---

### 10. **Scripts (CLI скрипты)** 
📁 `marker/scripts/`

#### **marker_single (convert_single.py)**
Конвертация одного файла:
```bash
marker_single /path/to/file.pdf
```

#### **marker (convert.py)**
Batch конвертация папки:
```bash
marker /path/to/folder --workers 4
```

#### **marker_chunk_convert (chunk_convert.py)**
Multi-GPU batch конвертация:
```bash
NUM_DEVICES=4 NUM_WORKERS=15 marker_chunk_convert input/ output/
```

#### **marker_gui (streamlit_app.py)**
Интерактивный веб-интерфейс:
```bash
marker_gui
```

#### **marker_server**
FastAPI сервер:
```bash
marker_server --port 8001
```

---

## Поток данных (Data Flow)

### Полный Pipeline конвертации:

```
1. ИНИЦИАЛИЗАЦИЯ
   ├─ Загрузка конфигурации (ConfigParser)
   ├─ Загрузка моделей Surya (create_model_dict)
   └─ Инициализация LLM Service (если --use_llm)

2. PROVIDER ФАЗА
   ├─ Определение типа файла (registry)
   ├─ Создание Provider (PdfProvider/ImageProvider/etc)
   ├─ Открытие файла
   └─ Извлечение базовой информации

3. BUILDER ФАЗА
   ├─ DocumentBuilder: создание Document
   ├─ LayoutBuilder: детекция layout → Block'и
   ├─ LineBuilder: детекция строк → Line блоки
   ├─ OcrBuilder: OCR текста → заполнение text в блоках
   └─ StructureBuilder: построение иерархии

4. PROCESSOR ФАЗА (последовательно)
   ├─ OrderProcessor: определение reading order
   ├─ LineMergeProcessor: объединение строк
   ├─ BlockRelabelProcessor: переклассификация блоков
   ├─ Специализированные процессоры:
   │  ├─ TableProcessor
   │  ├─ EquationProcessor
   │  ├─ SectionHeaderProcessor
   │  ├─ ListProcessor
   │  └─ ...
   ├─ LLM процессоры (если --use_llm):
   │  ├─ LLMTableProcessor
   │  ├─ LLMFormProcessor
   │  └─ ...
   └─ Финальные процессоры:
      ├─ TextProcessor
      └─ ReferenceProcessor

5. RENDERER ФАЗА
   ├─ Выбор renderer (Markdown/JSON/HTML/Chunks)
   ├─ Обход дерева блоков
   ├─ Генерация output
   └─ Извлечение изображений

6. OUTPUT ФАЗА
   ├─ Сохранение результата
   ├─ Сохранение изображений
   └─ Сохранение метаданных
```

---

## Иерархия блоков

```
Document
├─ Page 1
│  ├─ SectionHeader
│  ├─ Text
│  │  ├─ Line
│  │  │  └─ Span
│  │  │     └─ Char
│  │  └─ Line
│  ├─ Table
│  │  └─ TableCell
│  ├─ Figure
│  │  ├─ Picture
│  │  └─ Caption
│  └─ Equation
├─ Page 2
│  └─ ...
└─ ...
```

---

## Extensibility (Расширяемость)

### Добавление нового Processor:

```python
from marker.processors import BaseProcessor

class MyCustomProcessor(BaseProcessor):
    def __call__(self, document):
        # Обработка документа
        for block in document.contained_blocks((BlockTypes.Text,)):
            # Ваша логика
            pass
        return document
```

Затем передать в конвертер:
```python
converter = PdfConverter(
    processor_list=[MyCustomProcessor(), ...],
    ...
)
```

### Добавление нового Renderer:

```python
from marker.renderers import BaseRenderer

class MyCustomRenderer(BaseRenderer):
    def __call__(self, document):
        # Генерация вашего формата
        return output
```

### Добавление нового Provider:

```python
from marker.providers import BaseProvider

class MyFormatProvider(BaseProvider):
    def __init__(self, filepath):
        # Инициализация
        pass
        
    def get_page_count(self):
        # Реализация
        pass
```

Зарегистрировать в registry:
```python
ProviderRegistry.register(".myext", MyFormatProvider)
```

---

## Ключевые технологии

### Основные библиотеки:

1. **PyTorch 2.7+** - для запуска ML моделей
2. **Surya** - модели layout detection, OCR, reading order
3. **Pydantic** - валидация данных и настройки
4. **Click** - CLI интерфейс
5. **TQDM** - прогресс бары
6. **FastAPI** - API сервер (опционально)
7. **Streamlit** - веб интерфейс (опционально)
8. **Pillow** - обработка изображений
9. **pdftext** - извлечение текста из PDF
10. **python-docx** - работа с DOCX
11. **python-pptx** - работа с PPTX

### ML модели (Surya):

- **Layout Model** - распознавание структуры страницы
- **Detection Model** - детекция текстовых регионов и строк
- **Recognition Model** - OCR распознавание текста
- **Order Model** - определение порядка чтения
- **Table Recognition Model** - структура таблиц

---

## Паттерны проектирования

1. **Pipeline Pattern** - последовательная обработка через processors
2. **Strategy Pattern** - взаимозаменяемые renderers и providers
3. **Builder Pattern** - пошаговое построение документа через builders
4. **Registry Pattern** - автоматический выбор provider по расширению
5. **Composite Pattern** - древовидная структура блоков
6. **Factory Pattern** - создание моделей через `create_model_dict()`

---

## Многопоточность и GPU

### Batch обработка:
- **Multiprocessing**: параллельная обработка файлов (процессы)
- **Workers**: количество параллельных conversion процессов
- **GPU sharing**: каждый worker использует ~3.5GB VRAM

### Multi-GPU:
- **chunk_convert**: распределение файлов по нескольким GPU
- **NUM_DEVICES**: количество GPU
- **NUM_WORKERS**: workers на каждом GPU

### Оптимизации:
- Батчирование для inference моделей
- Переиспользование загруженных моделей
- Параллельная обработка страниц (где возможно)

---

## Debugging и мониторинг

### Debug режим (--debug):
- Сохранение изображений с визуализацией layout
- Детальные bounding boxes
- Логи всех промежуточных шагов
- JSON файл с полной структурой

### Логирование:
`marker/logger.py` - настройка логирования для всей системы

### Метрики:
- Время обработки каждой страницы
- Метод извлечения текста (pdftext/ocr/heuristic)
- Количество блоков каждого типа
- Статистика по страницам в метаданных

---

## Тестирование

📁 `tests/`

Comprehensive test suite:
- Unit тесты для builders
- Unit тесты для processors
- Unit тесты для renderers
- Integration тесты
- Benchmark тесты

Запуск тестов:
```bash
pytest tests/
```

---

## Производительность

### Типичные показатели (H100):

- **Одна страница**: ~0.18 секунд
- **Throughput (batch)**: ~122 страницы/секунду (22 параллельных процесса)
- **VRAM на worker**: ~3.5GB average, ~5GB peak
- **Проекция на batch**: 25 страниц/сек на H100

### Факторы влияния:
- Качество исходного документа
- Необходимость OCR
- Использование LLM (--use_llm добавляет время)
- Сложность layout (таблицы, формулы)

---

## Конфигурация и настройки

### Environment переменные:

```bash
TORCH_DEVICE=cuda  # cuda/cpu/mps
GOOGLE_API_KEY=xxx  # для Gemini
```

### Основные флаги CLI:

```bash
--use_llm              # Включить LLM для повышения точности
--force_ocr            # Принудительный OCR всего документа
--strip_existing_ocr   # Удалить существующий OCR и переделать
--output_format        # markdown/json/html/chunks
--page_range           # Диапазон страниц для обработки
--workers              # Количество параллельных workers
--debug                # Debug режим
--disable_image_extraction  # Не извлекать изображения
```

### JSON конфигурация:

```python
config = {
    "output_format": "json",
    "use_llm": True,
    "force_ocr": False,
    # ... другие настройки
}
config_parser = ConfigParser(config)
```

---

## Итоговая схема архитектуры

```
┌─────────────────────────────────────────────────────────┐
│                    CLI / API / GUI                       │
│              (scripts/convert.py, FastAPI,               │
│                    Streamlit)                            │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  ConfigParser                            │
│         (парсинг настроек, создание компонентов)         │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  Converter                               │
│         (PdfConverter, TableConverter, etc.)             │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
        ▼            ▼            ▼
┌──────────┐  ┌──────────┐  ┌──────────┐
│ Provider │  │ Builders │  │ Models   │
│ (данные) │  │ (структ.)│  │ (Surya)  │
└──────────┘  └──────────┘  └──────────┘
        │            │            │
        └────────────┼────────────┘
                     │
                     ▼
           ┌──────────────────┐
           │    Document      │
           │  (дерево блоков) │
           └─────────┬────────┘
                     │
                     ▼
           ┌──────────────────┐
           │   Processors     │
           │  (обработка)     │
           └─────────┬────────┘
                     │
            ┌────────┴────────┐
            │                 │
            ▼                 ▼
      ┌──────────┐      ┌──────────┐
      │ Basic    │      │ LLM      │
      │Processor │      │Processor │
      │          │      │(optional)│
      └────┬─────┘      └────┬─────┘
            │                 │
            └────────┬────────┘
                     │
                     ▼
           ┌──────────────────┐
           │    Renderer      │
           │  (генерация)     │
           └─────────┬────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
        ▼            ▼            ▼
   ┌────────┐  ┌────────┐  ┌────────┐
   │Markdown│  │  JSON  │  │  HTML  │
   └────────┘  └────────┘  └────────┘
```

---

## Заключение

Marker - это высокомодульная система с чистым разделением ответственности между компонентами. Каждый слой абстрагирован и может быть легко расширен или заменен. Pipeline построен как цепочка преобразований, где каждый шаг добавляет семантическую информацию к структуре документа, создавая в итоге богатое представление, которое можно отрендерить в различных форматах.

Ключевые преимущества архитектуры:
- ✅ **Модульность** - легко добавлять новые компоненты
- ✅ **Расширяемость** - поддержка custom processors, renderers, providers
- ✅ **Переиспользование** - модели загружаются один раз
- ✅ **Масштабируемость** - multi-process, multi-GPU support
- ✅ **Точность** - опциональная интеграция с LLM
- ✅ **Гибкость** - множество форматов ввода и вывода
